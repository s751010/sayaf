/**
 * أرقام المنتج المعروضة للزائر — **مصدر واحد**.
 *
 * ═══ لماذا وُجد هذا الملفّ ═══
 *
 * `Landing.tsx` كانت تكتب «١٢ طابعاً» بعدد ثابت، فبقيت ١٢ بعد إضافة سبعة
 * طوابع — أي أن الصفحة كانت **تنقص من المنتج سبعة أسباب للشراء** (§١٧). ثم
 * جاءت صفحة «من نحن» لتعرض الأرقام نفسها، فصار للرقم موضعان يتباعدان.
 *
 * ═══ لماذا `ALL_THEMES.length` ولا يُستورَد `lib/cards.ts` ═══
 *
 * `ALL_THEMES` في الحزمة الرئيسية أصلاً (يستوردها `MenuHeader`)، فقراءة طولها
 * مجانية. أمّا `lib/cards.ts` فحزمة مستقلّة ٤١ك تُحمَّل عند الطلب وحده (§١٧)،
 * واستيرادها هنا كان سيسحبها إلى كل زائر منيو مقابل رقمين — فتُكتبان يدوياً
 * مع تعليق يربطهما بمصدرهما، والفرق أن **الأكثر تغيّراً محسوب** والثابت مكتوب.
 */
import { ALL_THEMES } from "@/lib/themes";

/** عدد الطوابع الكاملة — محسوب، فلا يتقادم. */
export const THEME_COUNT = ALL_THEMES.length;

/** تخطيطات بطاقة الكاشير — `CARD_LAYOUTS` في `lib/cards.ts`. */
export const CARD_LAYOUT_COUNT = 4;

/** قياسات بطاقة الكاشير — `CARD_SIZES` في `lib/cards.ts`. */
export const CARD_SIZE_COUNT = 6;

/** دقّة ملفّ الطباعة — `mmToPx` في `lib/cards.ts`. */
export const PRINT_DPI = 300;

/* ── ألفاظ النداء ─────────────────────────────────────────────────────
 *
 * ⚠️ **لفظان لا أكثر، ومن هنا وحدهما.**
 *
 * كانت الصفحة تحمل اثني عشر لفظاً مختلفاً («ابدأ مجاناً» · «ابدأ الآن» ·
 * «أنشئ منيوك الآن» · «جهّز بطاقتك مجاناً» · «شوفه أولاً» · «افتح هذا
 * الطابع» …) لفعلين اثنين فقط: سجّل، أو شاهد. والزائر أمام أسماء كثيرة
 * لشيء واحد لا يختار أوّلها — يختار **لا شيء**.
 *
 * ⚠️ ولا «مجاناً» في اللفظ: لا طبقة مجانية في المنتج، والمجاني هو التجربة
 * وحدها. فتُقال مدّتها نصّاً مساعداً **تحت** الزرّ لا على وجهه — وعدٌ على
 * زرّ لا يفي به المنتج أغلى من وعدٍ لا يُقال.
 */

/** الفعل الأوّل: التسجيل. نفس لفظ الترويسة، فلا يتعدّد النداء بتعدّد الموضع. */
export const CTA_PRIMARY = "ابدأ تجربتك";

/** الفعل الثاني: مشاهدة المنتج قبل التسجيل. */
export const CTA_SECONDARY = "جرّب منيو تجريبي";
