/**
 * شاهدة قبر — `generate-article` مُعطّلة عمداً (2026-08-22).
 *
 * مولّد مقالات لمنصّة «مرشد السلامة» (HSE) — لا علاقة له بكلاود منيو.
 * حراستها كانت سليمة (مؤسّس فقط)، فالإقبار **تنظيف حدود لا سدّ ثغرة**:
 *
 * ١. منتج آخر يسكن مشروع كلاود منيو — وهو عين سبب النقل (§٢ في CLAUDE.md),
 *    ودالّته تحمل مفتاح الخدمة لقاعدة فيها بيانات التجّار ومفاتيح PayLink.
 * ٢. بريد المؤسّس **مكتوب بيدٍ ثانية** فيها بدل `founder_email()` في
 *    القاعدة — مصدران للهوية يتباعدان يوم يتغيّر البريد.
 *
 * لا تقرأ سرّاً، ولا تنادي شبكة، ولا تقرأ جسم الطلب.
 *
 * المصدر الأصلي محفوظ في المستودع:
 *   `supabase/functions/_archive/generate-article.index.ts.txt`
 *
 * ✅ للمالك: احذفها نهائياً من لوحة Supabase ← Edge Functions.
 */

Deno.serve(() =>
  new Response(
    JSON.stringify({
      error: "هذه الدالة مُعطّلة ولم تعد متاحة.",
      gone_at: "2026-08-22",
    }),
    { status: 410, headers: { "Content-Type": "application/json" } }
  )
);
