/**
 * شاهدة قبر — `OpenAI` مُعطّلة عمداً (2026-08-22).
 *
 * ⚠️ **الثغرة الحرِجة في فحص ٢٢/٠٨.** وسيط OpenAI مفتوح بلا حراسة إطلاقاً:
 * لا فحص مستخدم، ولا حدّ معدّل، ولا تقييد نموذج، ولا سقف إنفاق — والجسم
 * يُمرَّر كما هو إلى `api.openai.com` بمفتاح المالك.
 *
 * و`verify_jwt: true` **لا تحميه**: هي تقبل أي JWT موقَّع بمفتاح المشروع،
 * ومفتاح `anon` واحد منها — وهو منشور في حزمة جافاسكربت التي ينزّلها كل
 * زائر. أُثبت حيّاً بنداء واحد بمفتاح anon العام فردّت OpenAI ردّ طلبٍ
 * **موثَّق** ("you must provide a model parameter") لا 401.
 *
 * تخصّ منصّة HSE. إن عادت فمكانها مشروعها هي، وبحراسة.
 *
 * لا تقرأ سرّاً، ولا تنادي شبكة، ولا تقرأ جسم الطلب.
 *
 * المصدر الأصلي محفوظ في المستودع:
 *   `supabase/functions/_archive/OpenAI.index.ts.txt`
 *
 * ✅ للمالك: احذفها نهائياً من لوحة Supabase ← Edge Functions.
 */

Deno.serve(() =>
  new Response(
    JSON.stringify({
      error: "هذه الدالة مُعطّلة ولم تعد متاحة.",
      gone_at: "2026-08-22",
    }),
    { status: 410, headers: { "Content-Type": "application/json" } }
  )
);
