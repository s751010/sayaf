/**
 * شاهدة قبر — `ai-proxy` مُعطّلة عمداً (2026-08-05).
 *
 * كانت وسيط OpenAI لـ«المستشار الذكي» الذي حُذف من الواجهة، وبقيت ACTIVE
 * أشهراً بعد زوال ما يناديها.
 *
 * ═══ لماذا كانت خطرة ═══
 *
 * `system` و`messages` و`model` و`max_tokens` كلّها من جسم الطلب بلا تقييد
 * موضوع ولا حدّ معدّل. و`verify_jwt: true` **لا تعني «مستخدم مسجَّل»**: تعني
 * «أي JWT موقَّع بمفتاح المشروع» — ومفتاح `anon` منها، وهو منشور في حزمة
 * جافاسكربت التي ينزّلها كل زائر. أي أن أي زائر كان يستطيع تشغيل أي مطالبة
 * يريدها على حساب OpenAI الخاص بالمنصّة. صنبور فاتورة مفتوح.
 *
 * ═══ ⚠️ لماذا يوجد هذا الملفّ ═══
 *
 * الدالّة **منشورة حيّة** وتردّ ٤١٠ (مُتحقَّق منها في المشروع)، لكن لم يكن
 * لها **مصدر في المستودع** — لا هنا ولا في أي مكان. فإعادة نشر من الكود كانت
 * تسقطها بصمت، أو تعيدها بسلوكها القديم لو استُعيدت من الأرشيف بالخطأ.
 * والشاهدة هي المصدر الوحيد الذي يجب أن يُنشر تحت هذا الاسم.
 *
 * لا تقرأ سرّاً، ولا تنادي شبكة، ولا تقرأ جسم الطلب.
 *
 * المصدر الأصلي محفوظ في المستودع:
 *   `supabase/functions/_archive/ai-proxy.index.ts.txt`
 *
 * ✅ للمالك: احذفها نهائياً من لوحة Supabase ← Edge Functions.
 */

Deno.serve(() =>
  new Response(
    JSON.stringify({
      error: "هذه الدالة مُعطّلة ولم تعد متاحة.",
      gone_at: "2026-08-05",
    }),
    { status: 410, headers: { "Content-Type": "application/json" } }
  )
);
