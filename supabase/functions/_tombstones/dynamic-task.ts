/**
 * شاهدة قبر — `dynamic-task` مُعطّلة عمداً (2026-08-05).
 *
 * نسخة ثانية من وسيط OpenAI نفسه — وكانت مسجّلة باسم `ai-proxy` تحت slug
 * `dynamic-task`، واختلافُ الاسم عن الـslug أحدُ ما دلّ على أنّها أُنشئت على
 * عجل. تخصّ «المستشار الذكي» الذي حُذف من الواجهة.
 *
 * والخطر نفسه: وسيط OpenAI مفتوح بلا تقييد موضوع ولا حدّ معدّل، و`verify_jwt`
 * تعني «أي JWT موقَّع بمفتاح المشروع» — ومفتاح `anon` منها، وهو في حزمة كل
 * زائر.
 *
 * ═══ ⚠️ لماذا يوجد هذا الملفّ ═══
 *
 * الدالّة **منشورة حيّة** وتردّ ٤١٠ (مُتحقَّق منها في المشروع)، لكن لم يكن
 * لها **مصدر في المستودع**. فإعادة نشر من الكود كانت تسقطها بصمت. والشاهدة
 * هي المصدر الوحيد الذي يجب أن يُنشر تحت هذا الاسم.
 *
 * لا تقرأ سرّاً، ولا تنادي شبكة، ولا تقرأ جسم الطلب.
 *
 * المصدر الأصلي محفوظ في المستودع:
 *   `supabase/functions/_archive/dynamic-task.index.ts.txt`
 *
 * ✅ للمالك: احذفها نهائياً من لوحة Supabase ← Edge Functions.
 */

Deno.serve(() =>
  new Response(
    JSON.stringify({
      error: "هذه الدالة مُعطّلة ولم تعد متاحة.",
      gone_at: "2026-08-05",
    }),
    { status: 410, headers: { "Content-Type": "application/json" } }
  )
);
