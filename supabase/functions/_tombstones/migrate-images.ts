/**
 * شاهدة قبر — `migrate-images` مُعطّلة عمداً (2026-08-22).
 *
 * أداة نقل لمرّة واحدة كُتبت أثناء نقل المشروع: نقلت ستّ صور من تخزين
 * المشروع القديم ومن أعمدة `data:` base64 إلى دلاء الجديد. أدّت عملها
 * ورمزها في `internal_secrets` حُذف بعدها.
 *
 * ⚠️ أُقبرت **رغم عجزها**: دالّة تحمل مفتاح الخدمة وتكتب في التخزين وفي
 * ثلاثة جداول لا تُترك منشورة بعد انتهاء مهمّتها.
 *
 * لا تقرأ سرّاً، ولا تنادي شبكة، ولا تقرأ جسم الطلب.
 *
 * المصدر الأصلي محفوظ في المستودع:
 *   `supabase/functions/_archive/migrate-images.index.ts.txt`
 *
 * ✅ للمالك: احذفها نهائياً من لوحة Supabase ← Edge Functions.
 */

Deno.serve(() =>
  new Response(
    JSON.stringify({
      error: "هذه الدالة مُعطّلة ولم تعد متاحة.",
      gone_at: "2026-08-22",
    }),
    { status: 410, headers: { "Content-Type": "application/json" } }
  )
);
