# OS / editor
.DS_Store
Thumbs.db
*.swp

# Logs / temp
*.log
*.tmp
tmp/
*.tmp.js

# Dependencies / build (not used today, but guards future tooling)
node_modules/
.netlify/
dist/

# graphify — مخرجات تحليل مولّدة (graph.json/graph.html) وذاكرة الاستخراج
# تُعاد بالأمر: graphify app  — لا داعي لحفظها في المستودع
graphify-out/
