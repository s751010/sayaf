# تحليل شامل ومقارنة بين النسخ الثلاث — كلاود منيو

> تاريخ التحليل: 2026-07-26 · الفرع: `claude/comprehensive-analysis-comparison-qhgz11`
>
> **المرجع «الرسمي»** هو الملف الذي زوّده المالك، وقد تبيّن أنه **مطابق بايت-ببايت**
> لـ `legacy/public/index.html` (بصمة MD5 `3a785ba2393d1952968c033777c012d6`).

---

## 0. ملخّص تنفيذي (اقرأ هذا أولاً)

| | **الرسمي** `legacy/public/index.html` | **`web/`** (Next.js 16) | **`app/` + `deploy/`** (Vite SPA) |
|---|---|---|---|
| الحالة الفعلية | **يعمل على الإنتاج** (حسب المالك) | مرجع/بديل، لا يُنشر | **ما يبنيه `netlify.toml`** |
| الحجم | ملف واحد 1.49MB مصغّر | كود مصدري (~130 ملف) | كود مصدري (35 ملف) + `deploy/` |
| البناء | لا يوجد (يُنشر يدوياً) | ✅ يمر (`tsc` + `next build`) | ✅ يمر (`tsc` + `vite build`) |
| الباقات | **3** (49/99/199) | 2 (99/199) | 2 (99/199) |
| المصادقة | GoTrue يدوي + `localStorage` | Supabase Auth بالكوكيز + SSR | GoTrue يدوي + `localStorage` |
| تفعيل الاشتراك | **من المتصفح مباشرة** ⚠️ | `moyasar-webhook` | `moyasar-webhook` |

### ⚠️ التناقض الأهم في المستودع

`CLAUDE.md` يقول إن `legacy/` **«أرشيف لا يُطوَّر ولا يُنشر»**، والمالك يقول إنه **الموقع الرسمي الحيّ**.
في الوقت نفسه `netlify.toml` بالجذر يبني `app/` وينشر `deploy/`.
**النتيجة:** أي بناء يُطلقه Git على Netlify سيستبدل الموقع الرسمي بنسخة v2 — وهي نسخة
تنقصها ميزات جوهرية موجودة في الرسمي (السلة، الطلب عبر واتساب، التقييمات، أكواد الخصم،
الإعلانات، الدعم الفني). يجب حسم هذا قبل أي شيء آخر.

كذلك رابط الموقع غير موحّد: الرسمي و`app/` و`deploy/` تستخدم **`cloudsmenu.netlify.app`**،
بينما `web/src/lib/site.ts` و`CLAUDE.md` تستخدمان **`cloudmenuy.netlify.app`**.

---

## 1. تشريح النسخة الرسمية (`legacy/public/index.html`)

### 1.1 البنية التقنية
- ملف HTML واحد: 1,462,069 حرفاً / 1,948 سطراً.
- سكربت inline واحد بحجم ~1.44MB يحوي مضمّناً: React 19، Redux Toolkit، Immer،
  Recharts (رسوم بيانية)، مكتبة `qrcode`، وdecimal.js.
- CSS inline (لا ملف خارجي)، خطوط من **Google Fonts** (`preconnect` + `@import` معاً — تكرار).
- SPA يقرأ slug المطعم من `location.pathname` مع `_redirects` (`/*  /index.html  200`).
- PWA: `sw.js` (network-first للتنقّل، stale-while-revalidate للخطوط، لا يلمس Supabase/Moyasar) + `manifest.json`.
- رؤوس أمنية في `_headers`: CSP، `X-Frame-Options`، `nosniff`، `Permissions-Policy`. CSP مضبوطة بشكل صحيح مع `cdn.moyasar.com` و`*.supabase.co`.

### 1.2 الاتصال بالخلفية
- نداءات `fetch` يدوية لـ PostgREST و GoTrue — لا SDK.
- الجلسة في `localStorage` تحت مفتاح `cm_session`، والتسجيل عبر `/auth/v1/token?grant_type=password` و`/auth/v1/signup`.
- **مفتاح anon مكرّر 5 مرات** في الحزمة (`le`, `ke`, `NL`, `BL`, `ZL`) — مصادر متعددة لنفس الثابت.
- دوال الحافة المستخدمة: `ai-proxy` و`founder-admin`.
- الجداول المستدعاة: `restaurants`, `menus`, `dishes`, `analytics`, `subscriptions`,
  `announcements`, `promo_codes`, `support_tickets`, `revenue_log`, `site_settings`,
  `blog_posts`, `loyalty_customers`، **و`survey_responses`**.

### 1.3 خريطة الميزات

**صفحة المنيو العامة (للزبون)**
- عرض المنيو بالثيمات (12 ثيم + ثيم AI مخصّص مخزَّن كـ `ai:{json}`).
- **سلة تسوّق + إرسال الطلب على واتساب** بصيغة منسّقة، مع **رقم الطاولة** (`?table=` → `sessionStorage.cm_table`).
- `OptionsModal` — خيارات/إضافات لكل طبق مع فروق أسعار.
- **زر «ادفع الآن»** عبر بوابة الدفع الخاصة بالمطعم (مفتاح Moyasar للمطعم نفسه) + Apple Pay.
- **`SurveyModal`** — استبيان رضا من 5 أسئلة (`overall`, `food`, `service`, `speed`, `return`) + ملاحظة حرّة.
- `LangBtn` — تبديل عربي/إنجليزي (`name_en` / `description_en`).
- `LoyaltyCard` — بطاقة ولاء للزبون.
- ساعات العمل الأسبوعية، مسبّبات الحساسية، 5 روابط تواصل، رابط تقييم قوقل، مشاركة على تويتر.
- `DemoMenuPage` — منيو تجريبي بدون تسجيل.

**لوحة التاجر — 7 تبويبات:** `overview`، `menus`، `reviews`، `loyalty`، `ai`، `support`، `settings`
- المنيوهات: إنشاء/تعديل/حذف/**نسخ منيو كاملاً بأطباقه**، تفعيل/تعطيل، ثيمات.
- الأطباق: `name, description, price, category, emoji, image, featured, available, calories, sodium_mg, caffeine_mg, options` + `OptionsEditor`.
- QR: كود لكل منيو + **QR لكل طاولة حتى 60 طاولة مع طباعة دفعة واحدة**.
- الولاء (`LoyaltyPanel`): ختم زيارة، قائمة العملاء، باركود الولاء، إعدادات (هدف/مكافأة).
- التقييمات (`ReviewsPanel`): متوسطات لكل سؤال، نسبة الرضا، آخر 7 أيام، الملاحظات.
- AI — 6 تبويبات: محادثة، وصف طبق، اقتراح أسماء، ألوان تصميم، تحليل المنيو، **ترجمة المنيو**.
- الإعدادات: `WeeklyHoursEditor`، `allergens_text`، روابط التواصل، `google_review_url`،
  **`payment_gateway` + `payment_key`** (بوابة دفع خاصة بكل مطعم).

**لوحة المؤسس — 12 تبويباً:** `dashboard`، `users`، `announcements`، `promo`، `revenue`،
`expiring`، `support`، `team` (المجلس الاستشاري)، `health`، `activity`، `blog`، `footer`
- `FeatureToggles` — مفاتيح `orders_enabled` / `payment_enabled` في `site_settings.features`.
- `HealthPanel` — فحص صحة النظام (قاعدة البيانات، الدفع، AI، معدل التفعيل، تذاكر بلا رد، خطر Churn).
- `ActivityPanel` — سجل نشاط زمني موحّد (تسجيلات + مدفوعات).
- محرر الفوتر، محرر المدونة.
- الدخول عبر سر `cm_fsecret` في `sessionStorage` يُمرَّر كترويسة `x-founder-secret`.

### 1.4 التسعير في الرسمي
| المعرّف | الاسم | شهري | سنوي (معروض) | الحدود |
|---|---|---|---|---|
| `basic` | أساسي | 49 | 559 | منيو 1 · 40 طبق · بلا AI |
| `standard` | نمو | 99 | 1129 | 3 منيوهات · 100 طبق · AI |
| `premium` | احتراف | 199 | 2269 | 999 · 999 · AI |

---

## 2. تشريح `web/` (Next.js 16)

- App Router، React 19، Tailwind 4، `@supabase/ssr`، أيقونات lucide.
- **مصادقة Supabase حقيقية بالكوكيز** + تحديث الجلسة في `src/proxy.ts` (middleware).
- **حارسان للمؤسس**: `isFounder()` في التطبيق مقابل بريد `FOUNDER_EMAIL`، و`is_founder()` في سياسات RLS.
- تفعيل الاشتراك عبر **`moyasar-webhook`** (يُمرَّر `metadata` من نموذج الدفع) — لا كتابة اشتراك من المتصفح.
- **SSR حقيقي لصفحة المنيو** → `generateMetadata` لكل مطعم، `sitemap.ts`، `robots.ts`، `manifest.ts`.
- لوحة التاجر — 10 صفحات مستقلة: نظرة عامة، القوائم، الأصناف، أكواد QR، الإحصائيات،
  المستشار الذكي، الولاء، الاشتراك، الدعم الفني، الإعدادات.
- لوحة المؤسس — 7 أقسام: نظرة عامة، المطاعم والاشتراكات، الإعلانات، أكواد الخصم، الدعم، المدونة، الإعدادات.
- صفحة المنيو العامة: سلة + طلب واتساب، خيارات الأطباق، روابط تواصل، تتبّع مشاهدات (`view-beacon`)، `/demo`.
- حقول الأطباق أوسع: تُضاف `burn_minutes`, `allergens[]`, `name_en`, `description_en` فوق حقول الرسمي.
- التحقق: `npx tsc --noEmit` ✅ و`npm run build` ✅ (تم تشغيلهما في هذه الجلسة).

## 3. تشريح `app/` + `deploy/` (Vite SPA — ما ينشره `netlify.toml`)

- Vite + React 18 + TypeScript + Tailwind 4، 35 ملف مصدري (~3,539 سطراً).
- **بلا SDK**: `fetch` مباشر لـ PostgREST/GoTrue في `lib/api.ts` + `lib/session.ts`، جلسة `cm2_session`.
- **خطوط ذاتية الاستضافة** عبر `@fontsource` — لا اعتماد على Google Fonts (أفضل للخصوصية والسرعة).
- تقسيم الحزمة: المدونة/اللوحة/المؤسس `lazy`، وصفحة المنيو في الحزمة الرئيسية (229KB / 73KB مضغوطة).
- `ThemeToggle` — وضع فاتح/داكن (الرسمي داكن فقط).
- QR: كود المنيو + **QR لكل طاولة حتى 100 + طباعة دفعة**.
- القاعدة (أ) مطبّقة هيكلياً: `DishPayload` و`RestaurantSettingsPayload` هما مصدر الحقول الوحيد للإضافة والتحديث.
- **تم التحقق:** `deploy/` مطابق بايت-ببايت لناتج بناء نظيف من `app/` (البناء أُعيد في هذه الجلسة وبقيت شجرة git نظيفة).
- التحقق: `npm run build` (يشمل `tsc --noEmit`) ✅.

---

## 4. ما هو موجود في النسختين الأخريين وغير موجود في الرسمي

### 4.1 في `web/` (وأغلبه في `app/` أيضاً)

| # | الميزة/التحسين | الأثر |
|---|---|---|
| 1 | **تفعيل الاشتراك عبر `moyasar-webhook`** بدل الكتابة من المتصفح | يسدّ ثغرة الاشتراك المجاني (انظر 5.1) |
| 2 | **مصادقة Supabase بالكوكيز + SSR** بدل توكن في `localStorage` | مقاومة أفضل لـ XSS، وتجديد جلسة سليم |
| 3 | **حارس المؤسس ببريد مؤكَّد + `is_founder()` في RLS** | يزيل الاعتماد على سرّ مشترك واحد |
| 4 | **تسعير من مصدر واحد** (`lib/plans.ts`) + `resolvePlan()` + `entitlements` | يمنع تضارب الأسعار (انظر 5.3) |
| 5 | حقول أطباق إضافية: `burn_minutes`, `allergens[]`, `name_en`, `description_en` في whitelist صريح | الترجمة تُحفظ فعلاً (انظر 5.2) |
| 6 | `english_enabled` كمفتاح على مستوى المطعم مربوط بالباقة | تحكم أوضح بثنائية اللغة |
| 7 | كود مصدري قابل للقراءة والمراجعة بدل ملف مصغّر | صيانة ممكنة أصلاً |
| 8 | فحص أنواع + lint + بناء يمكن تشغيلهما في CI | الرسمي لا يمكن التحقق منه آلياً |

### 4.2 حصراً في `web/`

| # | الميزة | ملاحظة |
|---|---|---|
| 9 | **SEO حقيقي**: `generateMetadata` لكل مطعم، `sitemap`، `robots`، `canonical` صحيح | الرسمي و`app/` لا يملكانه (انظر 5.7) |
| 10 | صفحة `/dashboard/analytics` مستقلة، وصفحات أصناف مستقلة (`/dishes/new`, `/dishes/[id]`) | تنقّل وربط مباشر |
| 11 | إدارة اشتراكات المطاعم من لوحة المؤسس (`subscription-controls`) | تعديل يدوي لاشتراك أي مطعم |
| 12 | `upgrade-gate` موحّد لحجب ميزات الباقة الأعلى | |

### 4.3 حصراً في `app/` + `deploy/`

| # | الميزة | ملاحظة |
|---|---|---|
| 13 | **خطوط ذاتية الاستضافة** (@fontsource) | لا طلبات لجهات خارجية، أسرع وأأمن للخصوصية |
| 14 | **وضع فاتح/داكن** | الرسمي داكن فقط |
| 15 | نشر بلا متغيّرات بيئة ولا SSR (سحب مجلد) | أبسط نشر ممكن |
| 16 | QR لكل طاولة حتى **100** (الرسمي 60) + تصدير SVG | |
| 17 | `deploy/` ملتزَم في git ومتطابق مع المصدر | نشر يدوي فوري |

---

## 5. الأخطاء والمشاكل المكتشفة

### 🔴 حرجة — في النسخة الرسمية

#### 5.1 يمكن لأي مستخدم منح نفسه اشتراكاً مدفوعاً مجاناً
تفعيل الاشتراك يتم **من المتصفح**: بعد `on_completed` يُدخل الكود صفاً في `subscriptions`
وصفاً في `revenue_log` مباشرة بتوكن المستخدم. ونموذج Moyasar في الرسمي **لا يمرّر `metadata`
إطلاقاً**، أي أن دالة `moyasar-webhook` لا يمكنها التحقق أصلاً.
بما أن RLS على `subscriptions` مبنيّة على `auth.uid() = user_id`، فأي مستخدم مسجَّل يستطيع
تنفيذ `POST /rest/v1/subscriptions` بنفسه ويحصل على باقة «احتراف» بلا دفع.
**الإصلاح:** إسناد الإدراج لدالة `moyasar-webhook` (بمفتاح service role)، ومنع `INSERT/UPDATE`
على `subscriptions` من دور `authenticated` في RLS. النسختان الأخريان تفعلان ذلك بالفعل.

#### 5.2 باب خلفي للمؤسس مضمَّن في الكود العلني
```js
if (e === `i5nsi5nsi5nsi5ns@gmail.com`)
  return sessionStorage.setItem(`mh_admin_sess`, `admin-1`),
         sessionStorage.setItem(`cm_fsecret`, t), o(!0), { ok:!0, isAdmin:!0 };
```
إدخال هذا البريد في صفحة الدخول يفتح واجهة لوحة المؤسس **بأي كلمة مرور وبلا أي تحقق**،
وتُخزَّن كلمة المرور المكتوبة كـ `cm_fsecret`. البريد مكشوف لأي شخص يقرأ الحزمة.
حاجز البيانات الحقيقي يبقى سرّ `x-founder-secret` داخل دالة الحافة، لكن:
1. هذا البريد **يخالف** بريد المؤسس المعتمد `seeaf2013@gmail.com`.
2. كشف بنية اللوحة كاملة لأي زائر.
3. يُبقي بقايا تسمية قديمة (`mh_admin_sess`).
**الإصلاح:** حذف الشرط نهائياً والاعتماد على تحقق فعلي.

#### 5.3 السعر المعروض ≠ المبلغ المخصوم في الاشتراك السنوي
- بطاقات الأسعار تعرض السنوي: **559 / 1129 / 2269**.
- كود الدفع يستخدم: `const YR = {basic:399, standard:649, premium:949}`.

فمن يختار «احتراف — سنوي» يرى **2269 ر** ويُخصم منه **949 ر** فقط. وملخّص الدفع يعرض رقماً
ثالثاً («وفّرت `199*12 - 949` = 1439 ر»). ثلاثة أرقام لنفس العملية.
هذه خسارة **1,320 ر لكل اشتراك سنوي احترافي**، وتعارض واضح مع ما وُعد به العميل.
**الإصلاح:** مصدر واحد للأسعار (كما في `lib/plans.ts` بالنسختين الأخريين).

#### 5.4 أكواد الخصم لا تعمل إطلاقاً
لوحة المؤسس تُنشئ الأكواد وتحفظها في جدول **`promo_codes`** في Supabase،
لكن صفحة الدفع تتحقق منها من **`localStorage`**:
```js
var T = { ADMIN_NOTES: `cm_admin_notes` };   // لا يوجد مفتاح PROMO_CODES
var Me = e => (E(T.PROMO_CODES) || []).find(...)   // ⇒ localStorage.getItem(undefined) ⇒ []
```
`T.PROMO_CODES` غير معرَّف، فالنتيجة دائماً مصفوفة فارغة و**كل كود خصم يُرفض** مهما كان صحيحاً.
ودالة زيادة الاستخدام تكتب في `localStorage.setItem(undefined, …)`.
**الإصلاح:** قراءة `promo_codes` من Supabase مباشرة عند التحقق.

### 🟠 عالية — في النسخة الرسمية

#### 5.5 ميزة «ترجمة المنيو» مكسورة وتُبلغ عن نجاح كاذب
```js
yield a({ ...dsh, name_en: j.name, description_en: j.description || '' })
```
`a` هي `updateDish(id, payload)` وتأخذ **وسيطين**، لكنها تُستدعى بوسيط واحد → `payload`
تساوي `undefined` → استثناء عند `t.name`. الاستثناء يبتلعه `try{}catch(ex){}` داخل الحلقة،
ثم يظهر للمستخدم `✅` كأن العملية نجحت. وحتى لو صُحّح الاستدعاء، فإن `name_en` و`description_en`
**غير موجودين في whitelist التحديث** فسيُسقَطان بصمت — وهو بالضبط ما تحذّر منه القاعدة (أ) في `CLAUDE.md`.
النص المعروض تحت الزر يقول «تحفظ في `name_en`» — وهذا غير صحيح.

#### 5.6 لوحات التاجر تقرأ ببيانات اعتماد الزائر (anon) لا بتوكن المستخدم
```js
fetch(`${zL}/rest/v1/loyalty_customers?...`, { headers:{ apikey: le, Authorization:`Bearer ${le}` } })
```
`le`/`BL`/`ZL` كلها **مفتاح anon** وليس `access_token` المستخدم. ينطبق ذلك على
`loyalty_customers` و`survey_responses` و`announcements` وعدّاد `support_tickets`.
النتيجة إحدى حالتين، وكلتاهما مشكلة:
- **إن كانت RLS صحيحة** (`auth.uid() = user_id`): قوائم الولاء والتقييمات تظهر **فارغة دائماً**.
- **إن سمحت RLS بالقراءة للزائر**: أي شخص يستطيع قراءة **أرقام هواتف عملاء الولاء وتذاكر الدعم**
  لكل المطاعم — مخالفة خصوصية صريحة.
**مطلوب من المالك:** تشغيل `get_advisors(security)` وفحص سياسات هذين الجدولين لتحديد أي الحالتين قائمة.

#### 5.7 جدول `survey_responses` غير موجود في المخطط الموثّق
`CLAUDE.md` ينصّ صراحةً: «لا يوجد جدول `survey_responses`»، بينما الرسمي يكتب فيه ويقرأ منه.
والأسوأ أن `SurveyModal` يلتقط الفشل ويعرض «تم الإرسال» على أي حال:
```js
catch(ex){ setDone(true); }
```
فالزبون يظن أن تقييمه وصل، و`ReviewsPanel` يبقى فارغاً إلى الأبد.
**مطلوب:** إما إنشاء الجدول بسياسات RLS مناسبة، أو إزالة الميزة.

#### 5.8 دالة `founder-admin` غير منشورة على Supabase
سرد دوال الحافة الفعلية على المشروع `wjqpsbpebpntpeinqccl` أعاد **خمس دوال فقط**:
`ai-proxy`، `dynamic-task`، `notify-support`، `moyasar-webhook`، `payments`.
**لا يوجد `founder-admin`.** وهي الدالة التي تمرّ عبرها **كل** نداءات لوحة المؤسس في
النسخة الرسمية **و** في `app/` v2. إن صحّ ذلك فلوحة المؤسس معطّلة بالكامل في النسختين.
> ملاحظة: تعذّر إجراء اختبار HTTP حيّ للتأكيد (الشبكة الخارجية محجوبة في بيئة التنفيذ،
> واتصال Supabase MCP انقطع بعد سرد الدوال). يُرجى تأكيد ذلك من لوحة Supabase.
>
> يُلاحظ أيضاً تشوّش في التسمية: الدالة ذات الـ slug `ai-proxy` اسمها `OPENAI_API_KEY`،
> والدالة ذات الـ slug `dynamic-task` اسمها `ai-proxy` — يُستحسن ترتيبها.

### 🟡 متوسطة

#### 5.9 (الرسمي) `canonical` و`og:url` مثبّتان على الصفحة الرئيسية
```html
<link rel="canonical" href="https://cloudsmenu.netlify.app/">
<meta property="og:url" content="https://cloudsmenu.netlify.app/">
```
كل صفحة منيو `/{slug}` تعلن أن نسختها المعيارية هي الرئيسية → محركات البحث **لن تفهرس أي منيو**.
و`sitemap.xml` يحتوي رابطاً واحداً فقط. كذلك `og:image` ملف **SVG**، ولا تعرضه واتساب/تويتر/فيسبوك،
رغم `twitter:card = summary_large_image` — أي لا صورة معاينة عند مشاركة أي منيو.

#### 5.10 (`app/`) لا SEO إطلاقاً لصفحات المنيو
`app/index.html` بلا `canonical` وبلا `og:image` وبلا `og:url` وبلا JSON-LD، وهي SPA على العميل
بالكامل. النسخة التي ينشرها `netlify.toml` أضعف في SEO **من النسخة الرسمية نفسها**.

#### 5.11 (`app/`) تراجعات وظيفية مقابل الرسمي
النسخة التي يبنيها `netlify.toml` تفتقد ما يلي مما يعمل اليوم في الرسمي:
سلة التسوّق وإرسال الطلب على واتساب · استبيان التقييم ولوحة التقييمات · الإعلانات
· أكواد الخصم · تذاكر الدعم الفني · بوابة الدفع الخاصة بالمطعم · `FeatureToggles`
· `HealthPanel` و`ActivityPanel` ومحرر الفوتر · نسخ المنيو.
لوحة المؤسس في `app/` مختصرة إلى: إجمالي الإيراد، الاشتراكات النشطة، آخر 30 مطعماً، والتذاكر.

#### 5.12 تعارض التسعير بين النسخ الثلاث
| | باقة `basic` | `standard` شهري | حدود `standard` | `premium` سنوي |
|---|---|---|---|---|
| الرسمي | **49 ر — موجودة** | 99 | 3 منيوهات · 100 طبق · **مع AI** | 949 (المخصوم) |
| `web/` + `app/` | **محذوفة** | 99 | 1 منيو · 100 طبق · **بلا AI** | 2189 |

أي أن الانتقال إلى v2 يعني: إلغاء باقة الـ49، وسحب AI من الباقة المتوسطة، ومضاعفة السعر
السنوي للاحترافية أكثر من الضعف. هذه قرارات **تجارية** لا تقنية، ويجب أن تكون مقصودة لا عرَضية.
كما أن تعليق `plans.ts` يذكر أن `PRICES` في `moyasar-webhook` هي `{basic:49, standard:99, premium:199}`
— وهي **قيم شهرية فقط**؛ يجب التأكد من أن الدالة تقبل مبلغ الاشتراك السنوي (1089/2189)
وإلا فلن يُفعَّل أي اشتراك سنوي في v2.

#### 5.13 (الرسمي) JSON-LD يعلن سعراً وهمياً
```json
"offers": { "@type":"Offer", "price":"29", "priceCurrency":"SAR" }
```
لا توجد باقة بـ29 ريالاً في أي نسخة. أقل سعر فعلي 49.

### 🔵 منخفضة

- **(الرسمي)** `manifest.json` يحوي أيقونة **SVG فقط**؛ بعض إصدارات أندرويد تتطلب PNG بمقاسي 192/512
  لتفعيل «إضافة إلى الشاشة الرئيسية». `deploy/` يوفّر الأيقونتين.
- **(الرسمي)** تحميل الخطوط مكرّر: `<link rel=stylesheet>` **و** `@import` في `<style>` لنفس العائلات.
- **(الرسمي)** مفتاح anon مضمَّن 5 مرات بمتغيّرات مختلفة — مصادر متعددة لثابت واحد.
- **(الرسمي)** `console.log` و`console.error` متروكة في مسار الدفع (`Moyasar.init starting, amount:`).
- **(الرسمي)** بقايا تسمية قديمة: `mh_admin_sess`، `MH2025` كمثال لكود الخصم.
- **(الكل)** `NEXT_PUBLIC_MOYASAR_PK` / `MOYASAR_PK` لا يزال `pk_test_...` في النسخ الثلاث —
  مقصود بطلب المالك، لكنه يعني أنه **لا تُحصَّل أي مبالغ حقيقية اليوم**.
- **(`web/` مقابل الباقي)** `SITE_URL` غير متطابق (`cloudmenuy` مقابل `cloudsmenu`) — يؤثر على
  `sitemap` و`robots` و**روابط أكواد QR المطبوعة**.

---

## 6. الترتيب المقترح للمعالجة

1. **حسم أي نسخة هي المنشورة** وتعديل `netlify.toml` و`CLAUDE.md` ليعكسا الواقع — قبل أي بناء من Git.
2. إغلاق ثغرة الاشتراك المجاني (5.1) وحذف الباب الخلفي (5.2).
3. تأكيد وجود `founder-admin` وإعادة نشرها إن لزم (5.8).
4. توحيد الأسعار في مصدر واحد وإصلاح فارق السنوي (5.3)، وربط أكواد الخصم بقاعدة البيانات (5.4).
5. فحص سياسات RLS على `loyalty_customers` و`support_tickets` (5.6) وحسم مصير `survey_responses` (5.7).
6. توحيد `SITE_URL` وإصلاح `canonical`/`og:image` (5.9، 5.10).
7. إن كان الاتجاه هو الانتقال إلى v2: سدّ التراجعات الوظيفية في 5.11 أولاً.

---

## 7. ما تم التحقق منه فعلياً في هذه الجلسة

| البند | الطريقة | النتيجة |
|---|---|---|
| تطابق الملف المرفوع مع `legacy/` | `md5sum` | ✅ متطابق |
| بناء `app/` | `npm ci && npm run build` | ✅ نجح |
| تطابق `deploy/` مع مصدره | بناء نظيف ثم `git status` | ✅ متطابق بايت-ببايت |
| فحص أنواع `web/` | `npx tsc --noEmit` | ✅ بلا أخطاء |
| بناء `web/` | `npm run build` | ✅ نجح |
| دوال الحافة المنشورة | Supabase MCP `list_edge_functions` | ✅ 5 دوال، **بلا `founder-admin`** |
| مخطط الجداول وسياسات RLS | `execute_sql` / `get_advisors` | ❌ **تعذّر** (انتهت مهلة الاتصال ثم انقطعت المصادقة) |
| اختبار HTTP حيّ للدوال والجداول | `curl` | ❌ **تعذّر** (الشبكة الخارجية محجوبة في بيئة التنفيذ) |

البنود المعلَّمة ❌ تحتاج تأكيداً من المالك مباشرة عبر لوحة Supabase.
