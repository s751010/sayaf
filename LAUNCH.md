# قائمة الإطلاق — ما يبقى عليك أنت

هذا الملفّ **المصدر الوحيد** لما لا أستطيع تنفيذه من جلسة المساعد: إمّا لأنه
قرار تجاري، أو لأنه يحتاج بيانات لا يجوز أن أخترعها، أو لأنه يحتاج لوحة
Supabase / Netlify أو شبكةً محجوبة عن بيئتي.

> كل بند فيه: **لماذا** يهمّ · **كيف** يُنفَّذ · **كيف تتأكّد** أنه نفَذ.
> والترتيب بالأثر لا بالجهد.

**حالة القاعدة يوم كتابة هذا الملفّ (٦ أغسطس ٢٠٢٦):**
١٩ تاجراً · ١٩ حساباً · ١٨ اشتراك تجربة **كلّها تنتهي ١٦ أغسطس** ·
**صفر اشتراك مدفوع** · ١٣ مطعماً بصفر صنف · لا معرّف تحليلات ·
`enforce_publishing = false` · القاعدة في **طوكيو** (`ap-northeast-1`).

---

## 🔴 حاجز إطلاق — لا يُفتح الباب التجاري قبله

### ١) دفعة اشتراك حقيقية من طرف إلى طرف

**لماذا**: مسار الدفع كلّه مبنيّ ومُتحقَّق منه أمنياً (§٢١)، لكن **لم تمرّ
ريالٌ واحد فيه قطّ**: `subscriptions` فيه صفر صفّ مدفوع، و`revenue_log` فيه
ثمانية صفوف من تسعيرة قديمة. وأي عطل في التوصيل يظهر عند أول دفعة لا قبلها —
والأسوأ أن يظهر عند دفعة **تاجر** لا دفعتك أنت.

**كيف**:
1. Supabase ← Edge Functions ← Secrets: اضبط `PAYLINK_ENV=production`
   ومفاتيح الإنتاج (`PAYLINK_API_ID` و`PAYLINK_SECRET_KEY`).
2. من `/founder/money` ← وحدة الاستلام: انسخ رابط الويبهوك والصقه في لوحة
   PayLink. **الرابط يُشتقّ محلّياً ويظهر دائماً** — لا ينتظر نجاح فحص.
3. سجّل حساب تاجر تجريبياً بنفسك، واشترِ الباقة الشهرية (٩٩ ر.س) ببطاقة
   حقيقية.
4. استرجع المبلغ من لوحة PayLink بعد التأكّد.

**كيف تتأكّد** — الأربعة معاً لا واحد منها:
- صفّ في `subscriptions` بـ`plan_id='standard'` و`end_date` بعد ٣٠ يوماً.
- صفّ في `revenue_log` بـ**٩٩** (وللسنوي **١٠٨٩** = ١١ شهراً لا ١٢).
- الشارة في لوحة التاجر تقول «كلاود منيو» لا «تجربة».
- `/founder/money` ← وحدة الاستلام تعرض الدفعة تحت **«مسجَّل عندنا»**.

> ⚠️ لا تصدّق نجاح الخطوة من صفحة PayLink وحدها: الويبهوك عندهم **بلا توقيع**،
> فدالتنا تعيد سؤال PayLink بمفاتيحنا قبل أن تعتمد أي دفعة (§٢١). ونجاح
> الدفع عندهم لا يعني وصول الحدث إلينا.

### ٢) بيانات المنشأة النظامية في الصفحتين القانونيتين

**لماذا**: `/privacy` و`/terms` مكتوبتان من سلوك الشيفرة فعلاً وصحيحتان فيما
تصفانه، وفيهما **كتلة مُعلَّمة** تركتها فارغة عمداً — لا أخترع رقم سجلّ تجاري
ولا رقماً ضريبياً. وبيعُ اشتراك لمنشأة مجهولة الهوية ليس وضعاً تريد أن تكون
فيه.

**كيف**: عدّل `IDENTITY_TODO` في `app/src/pages/Legal.tsx` — الاسم النظامي ·
رقم السجل التجاري · الرقم الضريبي · العنوان الوطني · بريد التواصل. ثم
`npm run build`.

**كيف تتأكّد**: افتح `/terms` وابحث عن «بيانات المنشأة» — لا يبقى فيها أي نصّ
عنصر نائب.

---

## 🟠 مهمّ قبل التسويق أو خلاله

### ٣) انقل صور `data URI` إلى التخزين

**لماذا**: إحدى عشرة صورة مخزَّنة base64 **داخل أعمدة القاعدة**، وتصل
**كاملةً** في ردّ JSON حاجز لصفحة المنيو — لا كصورة كسولة. ثلاثة مطاعم يدفع
زائرها **٤٦٤–٥٣٩ كيلوبايت** ثمناً لذلك قبل أن يرى صنفاً. (وقد حُذف
`menus.cover_image` من الاستعلام العام لأنه لا يُعرض أصلاً — فسقط ١١١–١٦٢ك من
هذا الرقم بلا ترحيل.)

**لماذا لم أنفّذه**: رفع البايتات يحتاج Storage API على `<ref>.supabase.co`،
وسياسة الشبكة في بيئتي تحجب النطاق (`CONNECT tunnel failed, 403`). وأدوات SQL
المتاحة تكتب صفوفاً لا ملفّات.

**كيف**:
```bash
export SUPABASE_URL="https://wjqpsbpebpntpeinqccl.supabase.co"
export SUPABASE_SERVICE_ROLE_KEY="..."      # Settings ← API ← service_role

node app/scripts/migrate-data-uris.mjs           # فحص جافّ أولاً
node app/scripts/migrate-data-uris.mjs --apply
```
ثم **الصق `app/scripts/migrate-data-uris.owner.sql` في محرّر SQL** — الرفع
بمفتاح الخدمة يترك `owner` فارغاً، وسياسات التخزين (§٢٤) تشترط
`owner = auth.uid()` للاستبدال والحذف. **الترحيل لا يكتمل بدون هذه الخطوة**،
وبدونها لن يستطيع التاجر استبدال صورته لاحقاً.

**كيف تتأكّد**: افتح منيو `مشراق-0e94` وراقب حجم ردّ `dishes` في أدوات
المطوّر — بضعة كيلوبايتات لا مئات. والاستعلام:
```sql
select count(*) from public.dishes where image like 'data:%';   -- المتوقّع 0
```
السكربت **آمن للاستئناف**: يتخطّى ما صار رابطاً، ولا يمسح عموداً قبل أن يطلب
الرابط الجديد ويتأكّد من حجمه، ويحذف ما رفعه إن فشلت خطوة بعده.

**وبعد نجاحه فقط** — قيد يمنع عودتها من أي مسار كتابة (بما فيه API §١٤):
```sql
alter table public.dishes      add constraint dishes_image_not_data_uri
  check (image is null or image not like 'data:%') not valid;
alter table public.restaurants add constraint restaurants_logo_not_data_uri
  check (logo_image is null or logo_image not like 'data:%') not valid;
```
⚠️ **لا تضِفه قبل الترحيل**: فورم الطبق يعيد إرسال `image` كما هي، فتاجر يحرّر
طبقاً صورتُه data URI سيُمنع من الحفظ.

### ٤) انشر مقالات المدونة الثلاثة

**لماذا**: `/blog` كانت صفحة فارغة، ولذلك تستبعدها خريطة الموقع عمداً (إرشاد
قوقل إلى صفحة فارغة يضرّ). كتبتُ ثلاثة مقالات وأدخلتها **مسوّدات** — النشر
قرارك لأن المحتوى يخرج باسم علامتك.

المقالات: `qr-menu-in-30-minutes` (دليل البداية) · `sfda-calories-menu`
(السعرات والصوديوم) · `qr-menu-mistakes` (تجربة الزبون).

**كيف**: `/founder/comms` ← محرّر المدونة ← راجع كلّ مقال وفعّل «منشور».

> ⚠️ **راجع مقال السعرات خصوصاً**: كتبتُه شرحاً عملياً لا مرجعاً نظامياً،
> وفيه إشارة صريحة إلى ذلك. لم أخترع أرقام مواد ولا تواريخ أنظمة، لكن أي
> صياغة تلامس اشتراطات الهيئة يجب أن تمرّ عليك قبل النشر.

**كيف تتأكّد**: `/blog` تعرض الثلاثة، ثم `npm run build` ⇒ تُدرَج في
`sitemap.xml` تلقائياً (السكربت يضيف `/blog` **فقط** حين يوجد مقال منشور).

### ٥) معرّف التحليلات

**لماذا**: `lib/track.ts` يرصد ستة أحداث قمع (`signup_started` ⟵
`restaurant_created` ⟵ `first_dish_added` ⟵ `qr_downloaded` ⟵
`checkout_started` ⟵ `subscription_paid`)، و**لا يرسل بايتاً واحداً** بلا
معرّف. أي أنك اليوم تسوّق بلا أن تعرف أين يسقط الناس — و**١٣ من ١٩ توقّفوا
عند صفر صنف**، وهذا رقم لا تعرف سببه بلا قياس.

**كيف**:
```sql
insert into public.site_settings (key, value)
values ('analytics', '{"provider":"ga4","id":"G-XXXXXXX"}'::jsonb)
on conflict (key) do update set value = excluded.value;
```
⚠️ **وأضِف نطاق المزوّد إلى `app/public/_headers`** (المصدر — لا
`deploy/_headers` فهو ناتج بناء)، وإلا حجبته CSP بلا خطأ ظاهر للمستخدم.

**كيف تتأكّد**: افتح الموقع وراقب تبويب الشبكة — طلب واحد إلى نطاق المزوّد
وصفر خرق CSP في الـconsole.

### ٦) رابط استعادة كلمة المرور

**لماذا**: صفحة `/reset-password` مبنيّة وتعمل، لكن GoTrue **يرفض إعادة
التوجيه** إلى عنوان غير مُدرَج — فالرابط في البريد لا يعمل، والتاجر الذي نسي
كلمته يخسر منيوه.

**كيف**: Supabase ← Authentication ← URL Configuration ← Redirect URLs:
أضِف `https://cloudsmenu.netlify.app/reset-password` (وعنوان نطاقك المخصّص
حين يصير).

**كيف تتأكّد**: اطلب استعادة لحسابك واضغط الرابط فعلاً — لا تكتفِ بوصول
الرسالة.

---

## 🟡 تحسينات لها أثر واضح

### ٧) انقل القاعدة من طوكيو

**لماذا**: `ap-northeast-1` أي طوكيو، والرحلة إليها من السعودية **٢٠٠–٢٦٠
ملّي ثانية**. صفحة المنيو خُفّضت من أربع رحلات حاجزة إلى **رحلتين** (§٢٣)،
وهذا كسبٌ أكبر ممّا يمكن أن أكسبه بأي تحسين شيفرة إضافي — لكنه يبقى نصف ثانية
تُدفع على كل فتحة منيو، وزبونك على شبكة جوال داخل مبنى.

**كيف**: لا يوجد تحويل منطقة في المكان. المسار: أنشئ مشروعاً في
`eu-central-1` (فرانكفورت) أو `me-south-1` إن توفّرت ← انسخ المخطّط والبيانات
← حدّث `SUPABASE_URL` و`SUPABASE_ANON_KEY` في `app/src/lib/config.ts` ←
أعِد نشر دوال الحافة وأسرارها ← أعِد ضبط Redirect URLs (بند ٦).

**كيف تتأكّد**: `curl -w "%{time_total}" -o /dev/null -s` على
`/rest/v1/restaurants?limit=1` من جهاز في السعودية، قبل وبعد.

⚠️ اعمله في نافذة هادئة: هو **نقل** لا ترقية، ويعني توقّفاً قصيراً.

### ٨) النطاق المخصّص — **لا شيء عليك اليوم**

**الوضع الآن**: النطاق **تلقائي** — الموقع يبني كل رابط من المضيف الذي فُتح
عليه فعلاً. فوجّه أي نطاق تشتريه إلى Netlify ويعمل فوراً: أكواد QR والبطاقات
المطبوعة وcanonical وخريطة الموقع كلها تتبعه بلا تعديل سطر.

**لماذا هكذا**: كُتب النطاق نصّاً مرّة، وكان أثره أن كل كود QR يُطبع يحمل
النطاق المكتوب مهما نُشر الموقع عليه — فتوجيه نطاق جديد كان سيترك كل مطبوع
يشير إلى القديم. والتلقائي هو الافتراض الصحيح؛ وكتابة النطاق قرارٌ يُتَّخذ
**بعد** ربطه لا قبله.

**متى تخبرني**: بعد أن تشتري وتربط. حينها أكتب سطراً واحداً
(`MENU_DOMAIN = "..."` في `shared/menu-url.mjs`) فتُثبَّت كل الروابط عليه
بغضّ النظر عن مضيف اللوحة — وهو الأصحّ بعد أن يصير للمنصّة نطاق واحد معروف.

**وإن أردت نطاقاً فرعياً لكل مطعم** (`aldiwan.cloudmenu.sa` بدل
`cloudmenu.sa/aldiwan`) — الشيفرة جاهزة ومفحوصة، ويحتاج زيادةً:
1. بدل عامّ **`*.cloudmenu.sa`** في Netlify DNS + شهادة wildcard.
2. `MENU_MODE = "subdomain"` (سطر ثانٍ).
3. `https://<نطاقك>/reset-password` في Redirect URLs بـSupabase.

**كيف تتأكّد** بعد الربط: افتح منيو تاجر على النطاق الجديد ⇒ يعمل ·
و`canonical` عليه · و`sitemap.xml` بلا `netlify.app`.

> ⚠️ الروابط العربية القديمة (`مشراق-0e94`) **تبقى على شكل المسار** حتى في
> وضع النطاق الفرعي: لا تصلح تسمية DNS. ولمن يريد رابطاً نظيفاً: تغيير مرّة
> واحدة من الإعدادات، والقديم يُحوَّل ٣٠١ فلا ينكسر مطبوع.

### ٩) احذف حسابات الاختبار

**لماذا**: ١٣ من ١٩ مطعماً بصفر صنف. بعضها تاجر حقيقي توقّف (وهؤلاء **لا
يُحذفون** — يُتابَعون)، وبعضها تجاربك أنت. وهي تلوّث كل رقم في لوحتك: القمع،
ونسبة الاكتمال، و«يحتاج انتباهك».

**⚠️ لم ألمسها**: حذف حساب تاجر لا رجعة فيه، وتمييز «تجربتي» عن «تاجر توقّف»
يحتاج معرفتك بالأسماء والبريد لا استنتاجاً من البيانات.

**كيف**: افتح `/founder/merchants`، وميّز كلّاً منها بنفسك، ثم احذف من بطاقة
التاجر. والحذف يُسجَّل في `founder_audit`.

**كيف تتأكّد**: قبل الحذف اقرأ القائمة كاملةً:
```sql
select r.slug, r.name, u.email, r.created_at::date,
       (select count(*) from public.dishes d where d.restaurant_id = r.id) as dishes
from public.restaurants r join auth.users u on u.id = r.user_id
order by dishes, r.created_at;
```

### ١٠) فعّل حماية كلمات المرور المسرّبة

**لماذا**: صلّبتُ كلمة المرور عند الإنشاء (٨ محارف · تنوّع · قائمة شائعة —
§٢٤)، لكن هذا يصطاد الأنماط الشائعة لا الكلمات **المسرّبة فعلاً** في اختراقات
سابقة. و**لا قفل على دخول التاجر** (الاعتماد على حدود GoTrue)، فكلمة المرور
هي الحاجز الفعلي الوحيد.

**كيف**: Supabase ← Authentication ← Policies ← فعّل
«Leaked Password Protection» (تستخدم HaveIBeenPwned بهاش جزئي — الكلمة لا
تغادر).

**كيف تتأكّد**: جرّب التسجيل بكلمة مسرّبة معروفة ⇒ يُرفَض من الخادم.

### ١١) احذف الدالتين الميّتتين نهائياً

**لماذا**: `ai-proxy` و`dynamic-task` كانتا **وسيطَي OpenAI مفتوحين**
(`system` و`model` و`max_tokens` من جسم الطلب، و`verify_jwt` تعني «أي مستخدم
مسجَّل») — أي صنبور فاتورة مفتوح لكل تاجر. استبدلتهما بشاهدتَي قبر تعيدان
**٤١٠** ولا تقرأان سرّاً ولا تناديان شبكة، والمصدر مؤرشف في
`supabase/functions/_archive/`. الخطر مُطفأ، والباقي نظافة.

**كيف**: Supabase ← Edge Functions ← احذف الاثنتين (الحذف النهائي يحتاج
Management API المحجوب من بيئتي).

**كيف تتأكّد**: `list_edge_functions` لا تُرجعهما.

---

## ⚪ قرارات مفتوحة — لا شيء يتعطّل إن أجّلتها

| القرار | الوضع اليوم | ما يجب أن تعرفه قبل أن تقرّر |
|---|---|---|
| **قفل النشر** `enforce_publishing` | **`false`** | بُني ويبقى مطفأً عمداً. تشغيله يُطفئ منيو **كل** تاجر بلا اشتراك نشط، و١٨ تجربة تنتهي **١٦ أغسطس دفعةً واحدة**. لا تشغّله قبل أن يتحوّل ولو تاجر واحد إلى مدفوع، وقبل أن ترسل تنبيهاً |
| **تباين `light-luxe`** | `--m-muted` على `--m-bg` = **٣٫٦١:١** دون حدّ AA | الطابع مستعمَل على منيوهات فعلية، وتغيير لون طابع يعمل مخالف لثابت §١٨. القرار قرارك: تصحيح التباين أم إبقاء الطابع كما اعتاده تجّاره |
| **MFA** | غير موجود — ولا على لوحة المؤسّس | لوحة المؤسّس تفتح على بيانات ١٩ تاجراً. الجلسة في `localStorage` (افتراض Supabase)، وCSP هي المخفّف الوحيد |
| **سجلّ طلبات داخلي** | غير موجود | طلبات الزبائن تعيش في PayLink لا عندنا، ولهذا أُسقط حدث `order.paid` من الويبهوكات (§١٦) — وعدٌ لا يصل أسوأ من ميزة ناقصة. يعود متى وُجد السجل |
| **فاتورة ZATCA · التجديد التلقائي · الاسترجاع من اللوحة** | خارج النطاق (§٢١) | كلّها قرارات منتج لا نواقص تنفيذ |

---

## بعد أي تعديل في `app/src`

```bash
cd app && npm run typecheck     # صفر أخطاء
npm run build                   # يحدّث deploy/ — النشر يقرأ منه
```
ثابت **§١٨**: `git diff` على `lib/themes.ts` و`patterns.ts` و`fonts.ts` يجب
أن يكون **صفراً** — تجّار فعليّون على تلك القيم.
